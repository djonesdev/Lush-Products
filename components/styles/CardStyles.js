import styled from 'styled-components';

const Card = styled.div`
  background: white;
  box-shadow: var(--bs);
  border: 5px solid rgba(0, 0, 0, 0.05);
  border-radius: 1rem;
  padding: 1.5rem;
`;

export default Card;